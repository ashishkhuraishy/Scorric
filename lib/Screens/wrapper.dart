import 'package:flutter/material.dart';
import 'package:scorric/Screens/Authentication/authentication.dart';

class Wrapper extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Authentication();
  }
}